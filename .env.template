PORT=3001

# Database
DATABASE_URL=postgresql://user:password@host:5432/dbname
# Vercel Postgres defaults (used automatically if DATABASE_URL is unset)
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_PRISMA_URL_NON_POOLING=

# Firebase Auth (preferred)
FIREBASE_PROJECT_ID=gen-lang-client-0226043550
GOOGLE_APPLICATION_CREDENTIALS="C:/Users/Admin1/Desktop/AI Projects/SECRET KEYS/proveniq-tech-service-account.json"

# Legacy fallback (only if still needed)
JWT_SECRET=replace_with_long_random_string

# CORS
CORS_ORIGINS=http://localhost:5173,https://your-origins-domain.com

# Google OAuth (optional for login)
GOOGLE_DRIVE_CLIENT_ID=your_google_client_id
GOOGLE_DRIVE_CLIENT_SECRET=your_google_client_secret
GOOGLE_AUTH_REDIRECT_URI=http://localhost:3001/api/auth/google/callback
